    .my-icon-imsak::before {
        content: url('icon-1.png'); /* Replace with your image path */
        display: inline-block; /* Important for the image to render */
        width: 100%; /* Make the image fill the parent's width */
        height: 100%; /* Make the image fill the parent's height */
        background-size: contain; /* Ensures the image fits within the element */
        background-repeat: no-repeat; /* Prevents image repetition */
        background-position: center; /* Centers the image within the element */
    }

 .my-icon-subuh::before {
        content: url('icon-2.png'); /* Replace with your image path */
        display: inline-block; /* Important for the image to render */
        width: 100%; /* Make the image fill the parent's width */
        height: 100%; /* Make the image fill the parent's height */
        background-size: contain; /* Ensures the image fits within the element */
        background-repeat: no-repeat; /* Prevents image repetition */
        background-position: center; /* Centers the image within the element */
    }
